rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Same single-user gate as Firestore.
    match /{allPaths=**} {
      allow read, write: if request.auth != null
        && request.auth.token.email == 'dad@example.com';
    }
  }
}
