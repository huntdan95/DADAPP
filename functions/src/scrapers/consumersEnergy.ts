/**
 * Consumers Energy (Tippy + Hodenpyl on the Manistee). Stub.
 *
 * In practice the USGS gauge below Tippy (04125550) is the actionable
 * downstream signal, so a dedicated scraper here is low priority. If dad
 * specifically wants the planned-release schedule rather than the
 * after-the-fact gauge reading, this is where it lives.
 */

export const scrapeConsumersEnergy = null; // not yet implemented
