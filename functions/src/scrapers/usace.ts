/**
 * USACE district scrapers. Stub for future expansion.
 *
 * Each district (Sacramento, Tulsa, Nashville, etc.) publishes outflow data
 * at a district-specific URL — usually a CSV or HTML table at a stable
 * path. Implementation pattern: one module per district, same shape as
 * tva.ts, write to damSchedules/{damName_YYYY-MM-DD}.
 */

export const scrapeUsace = null; // not yet implemented
